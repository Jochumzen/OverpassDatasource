pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "OverpassDatasource"
include ':app'
include ':overpass_datasource'
include ':domain'
project(':domain').projectDir = new File('/../MapiDomain/domain')
